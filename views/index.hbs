<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Libro de Visitas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .container { max-width: 600px; margin-top: 50px; }
        .message-card { margin-bottom: 15px; }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4">📖 Libro de Visitas</h1>

        <!-- Formulario de envío -->
        <div class="card mb-4 shadow-sm">
            <div class="card-body">
                <form action="/nuevo-mensaje" method="POST">
                    <div class="mb-3">
                        <label for="usuario" class="form-label">Nombre</label>
                        <input type="text" class="form-control" id="usuario" name="usuario" required>
                    </div>
                    <div class="mb-3">
                        <label for="mensaje" class="form-label">Mensaje</label>
                        <textarea class="form-control" id="mensaje" name="mensaje" rows="3" required></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Enviar Mensaje</button>
                </form>
            </div>
        </div>

        <hr>

        <!-- Lista de mensajes -->
        <h3 class="mb-3">Mensajes Recientes</h3>
        {{#if mensajes.length}}
            {{#each mensajes}}
                <div class="card message-card shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title text-primary">{{this.usuario}}</h5>
                        <p class="card-text">{{this.mensaje}}</p>
                        <small class="text-muted">{{this.fecha}}</small>
                    </div>
                </div>
            {{/each}}
        {{else}}
            <p class="text-center text-muted">Aún no hay mensajes. ¡Sé el primero en escribir!</p>
        {{/if}}
    </div>
</body>
</html>
